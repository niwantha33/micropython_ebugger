rom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrom here (AFAIK)
        r = selrror(r)


__version__ = '0.1.2'
.ioctl(_MP_STREAM_FLUSH, 0)
        if r:
            raise OSError(r)


__version__ = '0.1.2'
